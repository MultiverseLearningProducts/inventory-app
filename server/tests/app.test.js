const { application } = require('express');
const Item = require(./item)
const app = require('./app');

DESCRIBE('inventory api', () => {
    ('GET item array', () => {});
    ('GET item/id', () => {});
    ('GET item/id -- 404 if not found', () => {});
    ('POST item, create item', () => {});
    ('GET item, validate request body' => {});
 
})